JavaScript 是属于网络的脚本语言！基本上成了html脚本语言首选.类似java,但不严格



HTML中的脚本必须位于 <script> 与 </script> 标签之间。脚本可被放置在 HTML页面的 <body> 和 <head> 部分中。外部的脚本通过<script>的src属性引入

语法:
	定义函数方法:
		function 方法名(参数){}
	定义变量:
		var 变量名={'属性':值;...}